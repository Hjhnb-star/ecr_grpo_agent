# ECR-GRPO Experiment Evidence Report

This report separates locally reproducible evidence from previously summarized results that are referenced in project documents but whose original table outputs are not currently present in this checkout.

## Local Artifacts Checked

The current local `runs/` directory contains smoke and pipeline-check outputs:

- `runs/baseline_check/comparison.csv`
- `runs/nonlocal_smoke/sweep_summary.csv`
- `runs/nonlocal_smoke/aggregate_by_kernel.csv`
- `runs/sweep_aggregate_smoke/sweep_summary.csv`
- `runs/sweep_aggregate_smoke/aggregate_by_kernel.csv`
- per-run `eval_metrics.csv`, `train_metrics.csv`, `credit_assignments.jsonl`, and `robustness_sweep.csv`

The following expected paper-table directories are not currently local:

- `runs/paper_tables/`
- `runs/hf_lora_stage_b_fair/`
- `runs/hf_lora_stage_b_lag1/`
- `runs/hf_lora_stage_b_lag3/`
- `runs/hf_lora_stage_b_ablation/`

Configs for Stage B are present under `configs/hf_lora_stage_b_*`, but the corresponding run outputs are absent.

## Locally Reproducible Smoke Evidence

### Baseline smoke

Source: `runs/baseline_check/comparison.csv`

| Kernel | Updates | Success | Avg steps | Avg return |
|---|---:|---:|---:|---:|
| trajectory | 2 | 0.000 | 9.125 | -0.253 |
| uniform | 2 | 0.000 | 9.125 | -0.231 |
| recency | 2 | 0.000 | 9.125 | -0.223 |
| dependency | 2 | 0.000 | 9.125 | -0.194 |

Interpretation: this is a pipeline smoke test, not a paper-quality result. It shows the kernels run and produce different returns, but the update budget is too small and all success rates are zero.

### Non-local smoke

Source: `runs/nonlocal_smoke/aggregate_by_kernel.csv`

| Kernel | n | Success | Return | Credit mass on causal steps | Positive credit frac | Entropy |
|---|---:|---:|---:|---:|---:|---:|
| recency | 1 | 0.000 | -0.287 | 0.765 | 0.080 | 2.546 |
| evidence | 1 | 0.000 | -0.279 | 0.712 | 0.090 | 2.540 |

Interpretation: evidence has a slightly better return in this local smoke run, but recency has higher causal credit mass. This run should not be used as the main attribution claim.

### Sweep aggregate smoke

Source: `runs/sweep_aggregate_smoke/aggregate_by_kernel.csv`

| Kernel | n | Success | Return | Credit mass on causal steps | Positive credit frac | Entropy |
|---|---:|---:|---:|---:|---:|---:|
| trajectory | 1 | 0.000 | -0.228 | 0.000 | 0.000 | 2.543 |
| evidence | 1 | 0.000 | -0.210 | 0.889 | 0.036 | 2.555 |

Interpretation: this supports the basic mechanism that evidence refill can place credit on causal steps while trajectory-level credit cannot, but it is still one-seed smoke evidence.

## Previously Summarized Stage A Evidence

Project documents report stronger Stage A results generated by `scripts/summarize_results.py` from `runs/paper_tables/`, but that directory is not present locally.

Reported Stage A main table:

| Method | Success | Return |
|---|---:|---:|
| Trajectory | 0.000 | -0.289 |
| Uniform | 0.383 | 0.338 |
| Recency | 0.675 | 0.474 |
| Evidence | 0.725 | 0.501 |

Reported credit diagnostic:

| Method | Target weight | Recent weight | Argmax target |
|---|---:|---:|---:|
| Evidence | 0.418 | 0.176 | 0.977 |
| Recency | 0.132 | 0.347 | 0.000 |
| Uniform | 0.188 | 0.166 | 0.441 |

Allowed claim if original `runs/paper_tables/` is restored:

> In controlled synthetic diagnostics, Evidence Refill improves non-local credit attribution and moderately improves success/return over Recency and Uniform.

Current local limitation:

> The original summary tables are referenced in `docs/stage_a_result_interpretation.md`, `docs/ecr_grpo_teacher_report.md`, and `docs/ecr_grpo_research_proposal.md`, but the local `runs/paper_tables/` folder must be restored or regenerated before final submission.

## Previously Summarized Stage B Evidence

Project documents report HF/LoRA synthetic fair comparison results, but local Stage B run outputs are not present.

Reported fair lag-2 result:

| Method | Final success | Logged mean success | Target weight | Recent weight | Argmax target |
|---|---:|---:|---:|---:|---:|
| Recency | 0.917 | 0.587 | 0.195 | 0.307 | 0.000 |
| Evidence | 0.889 | 0.540 | 0.623 | 0.095 | 1.000 |
| Gated | 0.917 | 0.658 | 0.755 | 0.080 | 1.000 |

Allowed claim if Stage B outputs are restored:

> In HF/LoRA synthetic training, pure Evidence improves non-local attribution but can dilute local learning, while Gated Evidence preserves Recency-level final success and improves non-local attribution.

Current local limitation:

> `configs/hf_lora_stage_b_*` exists, but `runs/hf_lora_stage_b_*` outputs are absent. The paper should treat Stage B numbers as previously summarized evidence until the run directories or generated `stage_b_summary.csv` files are restored.

## Stage C / ALFWorld Status

Stage C is not complete locally. The paper should not report ALFWorld success numbers until fair runs exist.

Minimum Stage C package:

| Required item | Status |
|---|---|
| Native ALFWorld sanity run | pending |
| Async-perturbed ALFWorld protocol | pending |
| GRPO-Trajectory baseline | pending |
| Step-GRPO / StepPO-style local baseline | pending |
| Recency, Evidence, Gated Evidence comparison | pending |
| Same seeds, policy, budget, and perturbation protocol | pending |
| Robustness AUC and attribution proxy logs | pending |

Allowed current wording:

> We include ALFWorld as a planned external-validity protocol.

Forbidden current wording:

> ECR-GRPO improves ALFWorld performance.

## Paper Table Plan

| Paper table / figure | Source needed | Current local status |
|---|---|---|
| Table 1: Synthetic main performance | `runs/paper_tables/main_performance.csv` | missing locally |
| Table 2: Credit diagnostic | `runs/paper_tables/credit_diagnostic.csv` | missing locally |
| Figure 4: Target vs recent credit | `credit_diagnostic.csv` and `credit_assignments.jsonl` | summary missing, raw smoke exists |
| Figure 5: Robustness curves | `runs/paper_tables/robustness_*.csv` | missing locally |
| Table 4: Evidence ablation | `runs/paper_tables/ablation_*.csv` | missing locally |
| Table 5: HF/LoRA fair comparison | `runs/hf_lora_stage_b_fair/stage_b_summary.csv` | missing locally |
| Table 6: ALFWorld comparison | new Stage C runs | pending |

## Recommended Next Experiment Actions

1. Restore or regenerate `runs/paper_tables/` from the server results referenced by the project documents.
2. Restore or rerun `runs/hf_lora_stage_b_*` and generate `stage_b_summary.csv` with `scripts/summarize_stage_b.py`.
3. Add a small manifest next to each paper table recording the command, config, seeds, and source run directories.
4. Do not move ALFWorld claims from protocol to results until Stage C run outputs exist.
5. Before final submission, run a data checker that verifies every number in `paper.md` has a source CSV or JSONL.
